"""Classical ML pipeline: ingest -> vectorize -> cluster -> route -> rank -> diversify.

No external model downloads. No hosted LLM calls. Everything is scikit-learn + numpy
fitted on this project's own corpus.
"""
